name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov

    # Pip-only installs
  - pip:
    - seamm_util>=0.9
    - seamm>=0.9
    - seamm-widgets>=0.9

      # Testing
    - yapf
    - codecov

