name: test

channels:
  - conda-forge

dependencies:
  # Base depends
  - python
  - pip

  # Requirements for this module
  - seamm

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Documentation
  - pydata-sphinx-theme
  - sphinx-design

  # Pip-only installs
  - pip:
    # Documentation
    - sphinx-copybutton
    - sphinxnotes-strike
